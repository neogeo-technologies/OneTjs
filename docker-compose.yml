version: '3'

services:
  onetjs:
    build:
      context: .
      dockerfile: docker/Dockerfile.apache
      #dockerfile: docker/Dockerfile.uwsgi
    environment:
      - SECRET_KEY='change me please'
      - ENV='production'
      - SERVER_NAME='127.0.0.1:8000'
      - TJS_SERVICE_ROOT_URL='http://127.0.0.1:8000'
    ports:
      - "127.0.0.1:8000:80"
