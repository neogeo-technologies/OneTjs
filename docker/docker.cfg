from decouple import config

CHOICES_TESTING = [ 'development', 'production']

SECRET_KEY = config('ONETJS_SECRET_KEY')
ENV = 'development' if config("ONETJS_ENV_DEV", default=False, cast=bool) else 'production'
TESTING = config('ONETJS_TESTING', default=False, cast=bool)
DATA_DIR_PATH = config('ONETJS_DATA_DIR_PATH', default="/onetjs/data")
SERVER_NAME = config('ONETJS_SERVER_NAME', 'localhost.localdomain:8000')
TJS_SERVICE_ROOT_URL = config('ONETJS_SERVICE_ROOT_URL', 'http://' + SERVER_NAME)
